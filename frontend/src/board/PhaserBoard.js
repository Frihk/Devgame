export class Board {
    constructor(scene) {
        this.scene = scene;
        this.tiles = [];
        this.createBoardGrid();
    }

    createBoardGrid() {
        console.log("Board mapping initialized.");
    }
}
